using Gtk 4.0;

template $CursorSizeSelector : ListBoxRow {
  activatable: false;

  child: FlowBox box {
    orientation: horizontal;
    homogeneous: true;
    selection-mode: none;
    max-children-per-line: 5;
  };
}
