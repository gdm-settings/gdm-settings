'''Information about the application itself got from the build system'''
from gettext import gettext as _

application_name = _("Login Manager Settings")
application_id   = 'io.github.realmazharhussain.GdmSettings'
project_name     = 'gdm-settings'
version          = '@version@'
build_type       = '@build_type@'

AppDir = __file__.split('@prefix@')[0]  # Directory which contains this app (detected during runtime)
data_dir = AppDir + '@data_dir@'        # Application-specific data directory

resource_base_path = '/io/github/realmazharhussain/GdmSettings/'
